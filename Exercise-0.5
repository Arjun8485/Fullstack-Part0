title New note diagaram 

note left of browser: User inpts data if any, then clicks submit (form default aciton is prevente).
browser->server:user input (POST request)
browser<--server:HTTP status code 201
note left of browser:The notes are rendered to the page
